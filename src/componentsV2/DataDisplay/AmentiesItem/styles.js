import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  amentiesItem: {
    paddingVertical: 20,
    flexDirection: 'row',
    alignItems: 'center',
  },
  image: {
    marginRight: 15,
  },
});
